<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:configChanges="uiMode"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PopUpActivity" />
        <activity android:name=".InviteRoom" />
        <activity android:name=".RoomAdapterActivity" />
        <activity android:name=".RateQuestionActivity" />
        <activity android:name=".QFactoryActivity" />
        <activity android:name=".GameDone" />
        <activity android:name=".GameActivity" />
        <activity android:name=".QSuggestionActivity" />
        <activity android:name=".FriendsRecyclerViewActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TrainingActivity" />
        <activity android:name=".CompetitiveActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Dashboard" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>